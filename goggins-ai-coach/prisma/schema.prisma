datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
  relationMode= "prisma"
}

generator client {
  provider = "prisma-client-js"
}


model Assistant {
  id String @id @default(uuid())
  assistantID String @unique
}